# Documentação de domínio

Este projeto usa um único contexto de domínio. Quando existir, `CONTEXT.md` na raiz conterá o glossário; decisões arquiteturais ficarão em `docs/adr/`. Antes de alterar o domínio, leia os documentos existentes. A ausência deles não bloqueia o trabalho.
